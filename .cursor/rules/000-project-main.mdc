---
alwaysApply: true
description: 项目全局架构、技术栈及目录规范
globs: **/*
---
# 技术栈约束 (核心)
- **后端**: Go 1.22+, 框架 [Gin Gonic](https://gin-gonic.com), ORM 使用 [GORM](https://gorm.io)。
- **数据库**: MySQL 9.0, 缓存使用 Redis (用于 Session/Token)。
- **测试**: Python 3.10+, [Pytest](https://docs.pytest.org), 异步请求使用 `httpx`。
- **并发**: 必须处理高并发场景，使用连接池和 Redis 缓存。

# 目录结构规范
- `/cmd`: 程序的入口 (main.go)。
- `/internal`: 内部业务逻辑 (service, repository, handler)。
- `/pkg`: 可复用的工具类。
- `/docs`: 存放 `api.md` 设计文档。
- `/tests`: 存放 Pytest 黑盒测试代码。

# 自动化要求
- 每次实现新接口，必须同步更新 `docs/api.md`。
- 每个接口必须对应至少一个 Python 测试用例。
